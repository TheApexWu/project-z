{
  "name": "lantern-order",
  "version": "0.1.0",
  "type": "module",
  "description": "A group order under one hard budget, with intra-group budget trading, settled by a Rain scoped card that is minted only once the group has finished deciding.",
  "scripts": {
    "test": "node --test",
    "gate": "node verify.mjs gate",
    "status": "node verify.mjs status",
    "preflight": "node --env-file=.env.local preflight.mjs",
    "drive": "node --env-file=.env.local drive.mjs --live",
    "serve": "node server.mjs"
  },
  "optionalDependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "viem": "^2.21.0"
  },
  "license": "MIT"
}
