{
  "project": "Zarathustra — real money-movement lanes (Rain sandbox + Monad x402)",
  "branch": "feat/live-settlement",
  "invariants": [
    "golden path stays mock + deterministic: node run.mjs reproduces Avesta head e5acaa05382d",
    "node --test green (48, incl the golden tripwire)",
    "FROZEN: src/engine.mjs, src/avesta.mjs, src/x402mock.mjs, and all test/* except golden.test.mjs",
    "live is OPT-IN; tests run keys-unset and fully offline",
    "push after EVERY green commit (loop runs on Carson's machine — remote monitoring)"
  ],
  "laneA_rain_PRIORITY": {
    "goal": "agents actually move money on Rain's card sandbox (wins the Rain tracks)",
    "api": "api-dev.raincards.xyz/v1 · docs rain-sandbox-trial.mintlify.site · SANDBOX ONLY",
    "milestones": [
      { "id": "A0", "goal": "preflight (blocks clean, no fake)", "files": ["rain.mjs"], "verify": "node --env-file=.env.local rain.mjs --check", "status": "built" },
      { "id": "A1", "goal": "fund collateral + issue a scoped card (the Gate)", "files": ["src/rainsandbox.mjs"], "verify": "rain.mjs --demo prints a real cardId", "status": "scaffolded — needs RAIN_* keys; confirm field names vs docs" },
      { "id": "A2", "goal": "authorize + settle a legit buy, readable back", "verify": "GET /issuing/transactions shows the settled txn", "status": "scaffolded" },
      { "id": "A3", "goal": "the in-policy flip: under-cap, wrong merchant in the ALLOWED MCC", "verify": "rain.mjs --demo shows the flip SETTLED (or DECLINED -> the card also binds the exact merchant; record which)", "status": "scaffolded — this result IS the pitch" },
      { "id": "A4", "goal": "score $-at-risk on the observed Rain transactions (the gauge on real data)", "status": "todo" }
    ]
  },
  "laneB_monad_SECONDARY": {
    "goal": "real x402/Monad settlement + the finality gap (the Mac Mini bounty)",
    "milestones": [
      { "id": "B0", "goal": "golden tripwire guards the head", "verify": "node --test", "status": "DONE" },
      { "id": "B1", "goal": "env preflight", "verify": "node --env-file=.env.local live.mjs --check", "status": "built" },
      { "id": "B2", "goal": "sign EIP-3009 (no broadcast), recovered signer == wallet", "verify": "live.mjs --dry-run", "status": "scaffolded — needs viem + creds" },
      { "id": "B3", "goal": "ONE real settlement, on-chain receipt (status success, from == wallet)", "verify": "ZARA_LIVE=1 live.mjs --live", "status": "scaffolded — confirm facilitator /settle shape Day-1" },
      { "id": "B4", "goal": "measure speculative-vs-final window -> reversibility (LIVE mode only)", "status": "todo" }
    ]
  },
  "prep": [
    "Rain: keys at the workshop desk (Api-Key, userId, teamId, contractId, sessionid) -> .env.local",
    "Monad: funded testnet wallet key + confirm RPC/USDC/facilitator constants + npm i viem",
    "run the first loop pass ATTENDED; keys in the live env only, never the test env; external wallet kill-switch"
  ]
}
